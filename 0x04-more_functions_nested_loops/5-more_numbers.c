#include <stdio.h>
#include "main.h"

/**
 * more_numbers - dfads
 */

void more_numbers(void)
{
	int x, y;

	for (y = 0; y < 10; x++)
	{
		for (x = 0; x < 15; x++)
			printf("%d", x);
		printf("\n");
	}
